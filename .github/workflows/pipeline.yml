name: Simple DevOps Pipeline

on: [push]

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Run Python script
      run: python script.py

    - name: Install pytest
      run: pip install pytest

    - name: Run tests
      run: pytest
